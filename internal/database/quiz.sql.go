// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.25.0
// source: quiz.sql

package db

import (
	"context"
	"database/sql"
	"time"

	"github.com/lib/pq"
)

const createQuiz = `-- name: CreateQuiz :one
INSERT INTO "quiz" (
  user_id, title, question_no, start_at, end_at, questions
) VALUES ($1, $2, $3, $4, $5, $6)
RETURNING id
`

type CreateQuizParams struct {
	UserID     int64         `json:"user_id"`
	Title      string        `json:"title"`
	QuestionNo int32         `json:"question_no"`
	StartAt    time.Time     `json:"start_at"`
	EndAt      sql.NullTime  `json:"end_at"`
	Questions  []interface{} `json:"questions"`
}

func (q *Queries) CreateQuiz(ctx context.Context, arg CreateQuizParams) (interface{}, error) {
	row := q.queryRow(ctx, q.createQuizStmt, createQuiz,
		arg.UserID,
		arg.Title,
		arg.QuestionNo,
		arg.StartAt,
		arg.EndAt,
		pq.Array(arg.Questions),
	)
	var id interface{}
	err := row.Scan(&id)
	return id, err
}
